#!/bin/bash
# Find this text

cd $p
# Create a list of links
t=/tmp/hammer-list$$

#d=$1
#[[ "$d" == "" ]] && d='.'

ls doc/$1 | 
while read f 
do
    echo "[[$f][$f]]"
done > $t

# Format the output
hammer-wiki $t

