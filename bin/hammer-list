#!/usr/bin/env python
# List a directory as a page

from os.path import join
from os import listdir, environ
from sys import argv

d = ''
if len(argv)>1: d = argv[1]
docdir = join(environ['p'], 'doc',d)
for f in sorted(listdir(docdir)):
    print  "[[%s][%s]]" % (join(d,f),f)
