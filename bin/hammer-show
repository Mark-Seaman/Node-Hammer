#!/bin/bash
# Return the wiki text from the requested file

# Handle directories
[[ -d "$1" ]] && hammer-list $1 && exit 0

# Handle missing page
echo "Missing page $1"

#Formatter is written in python
hammer-wiki $1

